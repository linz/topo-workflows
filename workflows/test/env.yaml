apiVersion: argoproj.io/v1alpha1
kind: Workflow
metadata:
  generateName: env-
spec:
  entrypoint: env
  templates:
    - name: env
      container:
        image: ubuntu:22.04
        command: [env]
